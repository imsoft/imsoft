---
import "../../styles/global.css";
import { services } from "../../data/services";
import ServiceCard from "./ServiceCard.astro";
---

<div class="bg-[var(--background)] py-12 sm:py-16">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-4xl text-center opacity-0 animate-fade-in">
      <h2
        class="text-4xl font-semibold tracking-tight text-[var(--foreground)] sm:text-5xl opacity-0 animate-fade-in-delay-200"
      >
        Impulsa tu negocio hoy
      </h2>
      <p
        class="mt-2 text-lg text-[var(--muted-foreground)] opacity-0 animate-fade-in-delay-400"
      >
        Estrategias digitales diseñadas para hacer crecer tu marca, atraer más
        clientes y generar resultados reales.
      </p>
    </div>

    <div
      class="mx-auto mt-16 grid max-w-2xl grid-cols-1 gap-x-8 gap-y-20
               lg:mx-0 lg:max-w-none lg:grid-cols-3"
    >
      {
        services.map((service, idx) => {
          const delay = idx === 0 ? 0 : idx * 200;
          return <ServiceCard service={service} delay={delay} />;
        })
      }
    </div>
  </div>
</div>
