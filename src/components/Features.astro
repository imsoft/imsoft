---
import { differentiators } from "../data/differentiators";
---

<div class="bg-[var(--background)] py-12 sm:py-16">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <div
      class="mx-auto grid max-w-2xl grid-cols-1 gap-x-8 gap-y-16 sm:gap-y-20 lg:mx-0 lg:max-w-none lg:grid-cols-5"
    >
      <div class="col-span-2 opacity-0 animate-fade-in">
        <h2 class="text-base/7 font-semibold text-[var(--primary)]">
          Lo Que Hace la Diferencia
        </h2>
        <p
          class="mt-2 text-4xl font-semibold tracking-tight text-[var(--foreground)] sm:text-5xl"
        >
          Estrategias que te hacen destacar
        </p>
        <p class="mt-6 text-base/7 text-[var(--muted-foreground)]">
          Soluciones digitales diseñadas para posicionarte por encima de la
          competencia. Innovación, eficiencia y resultados que impulsan tu
          negocio.
        </p>
      </div>

      <dl
        class="col-span-3 grid grid-cols-1 gap-x-8 gap-y-10 text-base/7 text-[var(--muted-foreground)] sm:grid-cols-2 lg:gap-y-16"
      >
        {
          differentiators.map((item, idx) => {
            const delayClass =
              idx === 0
                ? "animate-fade-in"
                : idx >= 5
                  ? "animate-fade-in-delay-800"
                  : `animate-fade-in-delay-${idx * 200}`;

            return (
              <div class={`relative pl-9 opacity-0 ${delayClass}`}>
                <dt class="font-semibold text-[var(--foreground)]">
                  <svg
                    class="absolute top-1 left-0 size-5 text-[var(--primary)]"
                    viewBox="0 0 20 20"
                    fill="currentColor"
                    aria-hidden="true"
                  >
                    <path
                      fill-rule="evenodd"
                      d="M16.704 4.153a.75.75 0 0 1 .143 1.052l-8 10.5a.75.75 0 0 1-1.127.075l-4.5-4.5a.75.75 0 0 1 1.06-1.06l3.894 3.893 7.48-9.817a.75.75 0 0 1 1.05-.143Z"
                      clip-rule="evenodd"
                    />
                  </svg>
                  {item.title}
                </dt>
                <dd class="mt-2">{item.description}</dd>
              </div>
            );
          })
        }
      </dl>
    </div>
  </div>
</div>
